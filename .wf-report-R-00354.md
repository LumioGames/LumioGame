# R-00354 implementer report

- Status: SUCCESS
- SHA: `6a2c09535b00fe1914dcbf5b1e2fa76b536c0b06`
- Branch: `feat/r-00354-e2e-harness` (not pushed)

## RED

Empty evidence pack (no round files):

```text
node integration/entity-chat/verify-evidence.mjs --dir integration/entity-chat/evidence/empty-pack
```

Exit 1. Failures include `census:bots` / `pack:rounds` (no per-entity host-audit, no round-2). Node tests assert empty packs fail 101 count, two-round compare, isolation, stale/tombstone, reconnect, snapshot-no-history.

## GREEN

```text
node --test integration/entity-chat/verify-evidence.mjs integration/entity-chat/bot-credential.mjs
```

12 pass / 0 fail.

```text
dotnet build modules/server-gameplay/src/Lumio.Game.EntityChat.Suite/Lumio.Game.EntityChat.Suite.csproj --nologo
Lumio.Game.ServerGameplay.Tests.exe
```

27 pass / 0 fail (includes 11 scenario unit tests). `dotnet test --project` still exit 5 / 0 tests on this machine.

```text
node .spec/tools/spec-lint.mjs   # OK
node --test .spec/tools/spec-lint.test.mjs   # 13 pass
```

## Both-run harness

```text
node integration/entity-chat/launcher.mjs --out integration/entity-chat/evidence/live-run
```

- account-server dll: `C:\Work\LumioGames\wt-server\r-00344\account-server\src\Lumio.Server.Account.App\bin\Release\net10.0\lumio-account-server.dll`
- round 1: exit 0, 100 bot + 1 player = 101, ~9s
- round 2: exit 0, same census / event order / applied Tick
- conclusion SUCCESS, launcher exit 0

Evidence (gitignored, no secrets in traces): `integration/entity-chat/evidence/live-run/{manifest.json,round-1,round-2}/` with `evidence.json`, `host-audit.ndjson` (`entity_admitted` per NetEntityId), `verify-report.json`.

Account flags: `--store-path <round>/account-store --listen 127.0.0.1:0` plus env `LUMIO_ACCOUNT_ADMISSION_PRIVATE_KEY_HEX` / `LUMIO_ACCOUNT_BOT_TOOL_PUBLIC_KEY_HEX` (generated test keys, not committed).

## Known gaps

- LumioServer `FullGraphComposition` still `MaxConnections = 64` / `MaxSessions = 64` at `mvp-host/src/Lumio.Server.MvpHost.App/FullGraphComposition.cs:30`. 101 live websocket admits would HTTP 503. ADR-0009: Game `GameRoomHost` + real Account Server process; does not claim mvp-host product-path capacity.
- Sibling `LumioServer/account-server/.../lumio-account-server.dll` is missing on the local origin/main checkout (behind). Live run used the r-00344 Release dll already on disk.
- Playwright Browser page is present under `integration/entity-chat/web/`; the SUCCESS run used Suite `browserWindow` evidence, not a live Chromium session.
- Knowledge: `entity-chat-harness.md` + ADR-0009. Not a spec-steward exemption.

## Commit

`feat(integration): add 101-entity entity-chat acceptance harness` (47 files). Not pushed.

---

# P0/P1 fix (review 退回)

- Status: BLOCKED
- SHA: `bfaaba53ec93cf4d1965f6bedf22eda9d2d091e9`
- Branch: `feat/r-00354-e2e-harness` (not pushed)

## RED

```text
node --test integration/entity-chat/verify-evidence.mjs integration/entity-chat/bot-credential.mjs
```

12 pass / 5 fail. New tests failed because GameRoomHost/fabricated `entity_admitted` packs were still SUCCESS (`ok: true`):

- suite-only host is not C# MVP host
- P1-1 scenarios[n].ok without mvp-host process audit
- P1-2 Browser ok without Playwright
- P1-3 HistoryCount default 0 is not a no-history test
- P1-5 cadence ok without Client Timer Manager

## GREEN

```text
node --test integration/entity-chat/verify-evidence.mjs integration/entity-chat/bot-credential.mjs
```

17 pass / 0 fail.

```text
node .spec/tools/spec-lint.mjs   # OK
node --test .spec/tools/spec-lint.test.mjs   # 13 pass
dotnet exec modules/server-gameplay/tests/Lumio.Game.ServerGameplay.Tests/bin/Debug/net10.0/Lumio.Game.ServerGameplay.Tests.dll
```

27 pass / 0 fail.

```text
node integration/entity-chat/launcher.mjs --out integration/entity-chat/evidence/live-run
```

- sibling host: `dotnet exec C:\Work\LumioGames\LumioServer\mvp-host\src\Lumio.Server.MvpHost.App\bin\Release\net10.0\lumio-mvp-host.dll` with `--listen ws://127.0.0.1:0 --allow-insecure-loopback --shared-secret-file … --reconnect-window-seconds 300 --enable-test-control --test-control-listen http://127.0.0.1:0 --audit-trace-file …/host-audit.ndjson`
- build command documented: `dotnet build --project C:\Work\LumioGames\LumioServer\mvp-host\src\Lumio.Server.MvpHost.App\Lumio.Server.MvpHost.App.csproj -c Release --nologo`
- live admits 64/101; 65th = HTTP 503
- conclusion BLOCKED, launcher exit 1
- `integration/entity-chat/evidence/live-run/blocked.json`: `FullGraphComposition.cs:30` `MaxConnections = 64 / MaxSessions = 64`, error `mvp-host HTTP 503`, `atConnection: 65`, `httpStatus: 503`

ADR-0009 superseded by ADR-0010. GameRoomHost is a unit-test double only. No r-00344 fallback.
